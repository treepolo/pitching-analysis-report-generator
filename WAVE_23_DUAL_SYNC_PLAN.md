# Wave 23 雙影片共用控制與同步

## 已選 UI 方案

採用方案一：雙影片下方一條橫跨兩側的共用控制列。播放、上一幀、下一幀、控制幀進度條、速度輸入／滑桿／重設，以及同步按鈕與同步資訊都屬於雙影片區塊層級；左右影片設定仍各自保留起點與終點。

## 行為契約

- 單影片維持既有單播放器控制列；雙影片不再各側各有一套控制列。
- 選定的是播放器區塊最外層；雙影片整個區塊為一個選定單位，亮框套在區塊外層。
- 同步按鈕以雙側目前顯示幀建立同步點，資訊顯示 Frame: X 與 Frame: Y。
- 每側起點／終點獨立判定；數值 0 代表未設定。同步點不得早於已設定起點，也不得晚於已設定終點。
- 同步後建立控制幀映射：控制幀 0 對應兩側有效播放區間起點，最後控制幀對應兩側有效播放區間終點；播放、逐幀、進度條、速度與循環只操作這組映射。
- 雙側任一方不在有效區間或沒有畫面時，共用播放停止；循環回到控制幀 0。
- 編輯器與輸出 HTML 使用相同的同步資料與控制行為；所有介面文字可選取與複製。

## 進度與驗證檢核

- [x] 修正輸出定位誤報與箭頭按鈕幾何。
- [x] 移除雙影片側內控制列，建立區塊層級共用控制列。
- [x] 建立雙影片同步資料結構、同步按鈕與同步資訊。
- [x] 實作雙側有效區間、同步點限制、控制幀映射、播放／逐幀／速度／循環。
- [x] 將編輯器與輸出 HTML 共用控制行為與可複製文字樣式。
- [x] 更新選定區塊與鍵盤快捷鍵作用域。
- [x] 補齊 renderer、export、runtime、同步邊界測試。
- [x] 執行完整測試、語法檢查、diff 檢查，建立並推送 Git checkpoint。

## 驗證紀錄

- npm test：146 tests，145 pass、0 fail、1 skipped。
- node --check：renderer、輸出 renderer、native runtime 全部通過；產生的 inline native script 亦通過 new Function 編譯。
- Electron file:// smoke test 因目前環境無可用 Electron runtime 而跳過；其餘 runtime/static export 驗證通過。
- Git checkpoint 與 push 於交付前完成。